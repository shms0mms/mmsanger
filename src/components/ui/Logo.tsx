import { FC } from "react"

const Logo: FC = ({}) => {
	return (
		<div className="flex items-center text-lg">
			<span className="font-bold">Mms</span>
			<span className="text-destructive">anger</span>
		</div>
	)
}

export default Logo
