query GetLastMessagesByChatId($chatId: String!) {
	getLastMessagesByChatId(chatId: $chatId) {
		message 
		chatId
		userId
		id
		user {
			firstName 
			secondName 
			status 
			imageURL
			id
		}
	}
}